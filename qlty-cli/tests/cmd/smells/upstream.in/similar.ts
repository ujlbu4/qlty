function foo(x: number): number {
  if (x > 10) {
    return x * 2;
  } else {
    return x + 1;
  }
}

function bar(y: number): number {
  if (y > 10) {
    return y * 2;
  } else {
    return y + 1;
  }
}
