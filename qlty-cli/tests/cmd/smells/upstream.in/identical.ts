function foo(x: number): number {
  if (x > 10) {
    return x * 2;
  } else {
    return x + 1;
  }

  while (true) {
    return x * 2;
  }
}

function bar(x: number): number {
  if (x > 10) {
    return x * 2;
  } else {
    return x + 1;
  }

  while (true) {
    return x * 2;
  }
}
